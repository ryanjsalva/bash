#   Node Version Manager (NVM), https://github.com/creationix/nvm
#   ------------------------------------------------------------

    export NVM_DIR="$HOME/.nvm"
    [[ -r $NVM_DIR/bash_completion ]] && . $NVM_DIR/bash_completion         # command completion
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"                         # source NVM
